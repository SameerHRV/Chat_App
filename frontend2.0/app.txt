<Dialog open>
      <Stack>
        <DialogTitle>Add Member</DialogTitle>

        <Stack>
          {sampleUsers.map((i) => (
            <UserItem key={i._id} user={i} handler={addFriendHandler} />
          ))}
        </Stack>
      </Stack>
    </Dialog>
